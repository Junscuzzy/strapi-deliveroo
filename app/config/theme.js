/**
 * Default Theme
 * @link https://material-ui.com/customization/default-theme/#default-theme
 */
import { createMuiTheme } from '@material-ui/core/styles'

const theme = createMuiTheme({
  palette: {
    primary: {
      main: 'rgb(0, 204, 188)'
    }
  }
})

export default theme
